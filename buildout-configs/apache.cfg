[buildout]
parts += apache-conf
extends = pound.cfg

[configuration]
www-domain = www.example.com
plone-site = Plone
server-admin = helpdesk@eionet.europa.eu

[apache-conf]
recipe = collective.recipe.template
input = ${buildout:directory}/buildout-configs/templates/apache.tpl
output = ${buildout:directory}/etc/apache-vh.conf
