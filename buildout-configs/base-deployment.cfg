[buildout]
extends =
    base-zope.cfg
    openldap.cfg
    zopesendmail.cfg
    memcache.cfg

parts +=
    restart-portal

[restart-portal]
recipe = collective.recipe.template
input = ${buildout:directory}/buildout-configs/templates/restart-portal.tpl
output = ${buildout:directory}/etc/rc.d/restart-portal