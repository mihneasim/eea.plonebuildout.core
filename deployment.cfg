[buildout]
extends =
    buildout-configs/apache.cfg
    buildout-configs/base-deployment.cfg

allow-picked-versions = false

parts += 
    apache-conf

develop +=


[configuration]
effective-user = zope
